import styled from 'styled-components';

const SelectedProduct = () => {
    return (
       <SelectedProductWrapper>
           Product
       </SelectedProductWrapper>
    );
};

export default SelectedProduct;

const SelectedProductWrapper = styled.section`

`